import { HeaderBackground } from "@react-navigation/stack";
import * as Notifications from "expo-notifications";

export const getProjectsConversations = async (project_id) => {};

export const getUserConversations = async (user_id, project_id) => {};

export const postQuestion = async (user_id, project_id, body) => {};

export const postReply = async (user_id, question_id, body) => {};
